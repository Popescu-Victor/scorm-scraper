import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('csv.csv')
users = list(df.iloc[:, 7])

for i in users :
  i = i.replace("Test Passes for Participant: ", "")
  print("\n" + str(i))
